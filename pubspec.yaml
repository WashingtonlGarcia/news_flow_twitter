name: news_flow
description: A new Flutter project.
publish_to: 'none'
# Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations: #Adicionado para ultilizar data em português
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_dotenv: ^2.1.0
  twitter_api: ^0.1.2
  intl: ^0.16.1

  flutter_icons: ^1.1.0
  loading_animations: ^2.1.0
  cached_network_image: ^2.2.0+1
  url_launcher: ^5.5.0

  #Firebase
  #cloud_functions: 0.4.1+8
  #cloud_firestore: 0.13.7
  dio: ^3.0.10
  #webview_flutter: ^0.3.22+1
dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.2.0

flutter:
  uses-material-design: true
  assets:
    - .env
